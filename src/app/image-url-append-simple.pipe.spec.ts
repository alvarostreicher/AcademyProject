import { ImageUrlAppendSimplePipe } from './image-url-append-simple.pipe';

describe('ImageUrlAppendSimplePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlAppendSimplePipe();
    expect(pipe).toBeTruthy();
  });
});

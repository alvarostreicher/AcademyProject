import { ImageUrlAppendPipe } from './image-url-append.pipe';

describe('ImageUrlAppendPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlAppendPipe();
    expect(pipe).toBeTruthy();
  });
});
